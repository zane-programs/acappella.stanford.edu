"""Builds the homepage hero loop (desktop and mobile) from YouTube footage.

Entry point: ``python3 tools/hero-video/build.py --help`` or ``npm run hero-video``.
Everything the edit needs (clips, framing, grade, encode targets) lives in
``tools/hero-video/loop.json``; the code in this package only executes it.
"""
